# Examples

This directory contains code examples demonstrating how to use `lib3mf-rs`.

## Running Examples

You can run an example using `cargo run --example`:

```bash
cargo run --example simple_read
```

## Available Examples

*   **`simple_read`**: Opens `models/Benchy.3mf`, parses it, and prints basic statistics.
